import { Text } from 'native-base';
import React from 'react';
import { StyleSheet, View } from 'react-native';

export const AccountOptions = (): JSX.Element => {
  return (
    <View>
      <Text>AccountOptions</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
