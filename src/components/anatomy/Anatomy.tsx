import { Container } from 'native-base';
import React from 'react';
import { Routes } from '../../router/Routes';

export const Anatomy = (): JSX.Element => {
  return (
    <Container>
      <Routes />
    </Container>
  );
};
